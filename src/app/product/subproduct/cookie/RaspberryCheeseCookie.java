package app.product.subproduct.cookie;

import app.product.subproduct.Cookie;

public class RaspberryCheeseCookie extends Cookie {
    public RaspberryCheeseCookie() {
        super("raspberryCheeseCookie", 1500, 350);
    }

    public RaspberryCheeseCookie(int id) {
        super(id, "raspberryCheeseCookie", 1500, 350);
    }
}
